1.  ```bash
    # instalar as dependências
    npm i
    ```
2. Crie um arquivo .env na raíz do projeto, e insira o valor VITE_API_URL=http://127.0.0.1:8000/api, onde esse domínio é o domínio da API backend django rodando
3. ```bash
    # Iniciar o servidor
    npm run dev
    ```
